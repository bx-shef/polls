{
  // Проект ТОЛЬКО для тестов проводки (приглашения + присвоение портала): они загружают модули Nitro, живущие на
  // авто-импортах Nuxt (`defineNitroPlugin`, `getRequestIP`, `import.meta.prerender`). Ядровой
  // `tsconfig.json` framework-agnostic и этих имён не знает — там эти файлы исключены. Без своего
  // проекта типовые правила ESLint по ним молча выродились бы (ровно тот отказ, из-за которого у
  // `test/fixtures` тоже свой конфиг), а `parserOptions.project` дал бы ошибку разбора.
  "extends": "../.nuxt/tsconfig.server.json",
  "include": [
    "./invitation-retention.test.ts",
    "./invitation-wiring.test.ts",
    "./install-wiring.test.ts",
    "./close-invite.test.ts",
    "./manual-invite.test.ts",
    "./post-result.test.ts",
    "./close-invite-wiring.test.ts",
    "./tenant-wiring.test.ts",
    "./demo-seed-wiring.test.ts",
    "./helpers/schema.ts",
    "../server/**/*.ts"
  ]
}
