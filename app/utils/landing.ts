// Продающие тексты: то, что видит покупатель. Кормят страницу лендинга (`app/pages/index.vue`) и
// разметку обложки для соцсетей (`scripts/lib/ogTemplate.mjs`) — поэтому обещание продукта не может
// разойтись между сайтом и картинкой. Правка `LANDING_TITLE`/`LANDING_DESCRIPTION`/`LANDING_METRICS`
// требует `pnpm og`. Тексты на изображениях карточки Маркета — в `docs/market-graphics.md`.
//
// ⚠️ **Главное правило этого файла: обещать только то, что отгружено.** Мерж в `main` публикует
// страницу на живой домен (авто-CD), а сервис ещё ни разу не прогонялся на реальном портале.
// Поэтому здесь НЕТ и до закрытия соответствующих задач быть не должно:
//  — «опрос уходит клиенту сам» / доставка приглашения в таймлайн сделки (#126, блокируется #138);
//  — «сводка ответа попадает в карточку сделки» (#18): код есть, но живого прогона не было ни разу,
//    а обещать на витрине непроверенное нельзя. ⚠️ Прежнее обоснование («пока не написан гейт
//    постинга») снято: гейт написан (`invitationPolicy.resultToTimeline`) и вынесен в конструктор.
//    Но он же меняет и правило про анонимность ниже: возврат результата в карточку ВКЛЮЧЁН по
//    умолчанию, значит витрина не вправе называть опрос анонимным без оговорки;
//  — «данные лежат на вашем сервере» — модель развёртывания у издателя, а не у клиента.
// Формулировки в настоящем времени описывают ПРОДУКТ, а строка `LANDING_STATUS` на странице
// говорит, что он ещё готовится к публикации, — чтобы ни одна фраза не читалась как гарантия.

export const LANDING_TITLE = 'Опросы клиентов в Bitrix24'

/** Подзаголовок для страницы — длинный, полностью описывает продукт. */
export const LANDING_SUBTITLE
  = 'Клиент проходит короткий анонимный опрос по ссылке из сделки, а руководитель открывает готовую аналитику: NPS и CSAT по клиентам, услугам, направлениям и сотрудникам, с трендом по месяцам — без выгрузок и ручных подсчётов.'

/**
 * Честный статус продукта, который стоит первой строкой страницы.
 *
 * Не мелкий шрифт у кнопки, а заметная строка в герое: страница уезжает на живой домен раньше, чем
 * приложение появится в Маркете, и посетитель должен понимать это до того, как прочтёт возможности.
 */
export const LANDING_STATUS = 'Готовится к публикации в Маркете Битрикс24'

/**
 * Короткое описание для карточек-превью (og:description и витрины).
 *
 * Своя строка, а не псевдоним подзаголовка: валидаторы карточек ждут 90–155 знаков, длинный
 * подзаголовок обрезается на середине фразы. Тот же урок, что и в `ai-price-import`.
 */
export const LANDING_DESCRIPTION
  = 'Анонимные опросы клиентов внутри Bitrix24: ссылка из карточки сделки, готовые NPS и CSAT по клиентам, услугам и сотрудникам — без выгрузок.'

/** Окно, в котором валидаторы карточек считают описание нормальным. */
export const DESCRIPTION_MIN = 90
export const DESCRIPTION_MAX = 155

/** Акцентный цвет приложения. У соседей по семейству — голубые тона (`#22d3ee`, `#00d4ff`),
 *  здесь фиолетовый: три карточки в ленте Маркета должны читаться как одно семейство и при этом
 *  не путаться между собой. ⚠ Заготовка — окончательный тон подтверждает дизайнер. */
export const BRAND_ACCENT = '#8b5cf6'
export const BRAND_ACCENT_SOFT = '#a78bfa'

/** Что показываем строкой под заголовком — не форматы файлов, как у соседей, а метрики. */
export const LANDING_METRICS: string[] = ['NPS', 'CSAT', 'Тренд по месяцам', 'Срезы по сотрудникам']

export interface LandingStep { n: number, title: string, text: string }
export const LANDING_STEPS: LandingStep[] = [
  { n: 1, title: 'Ссылка из карточки сделки', text: 'Менеджер создаёт её одной кнопкой; вместе со ссылкой сохраняется срез сделки — услуга, направление, ответственный.' },
  { n: 2, title: 'Клиент проходит опрос', text: 'Анонимно, без регистрации, за пару минут — с телефона или компьютера.' },
  { n: 3, title: 'Цифры собираются сами', text: 'NPS и CSAT считаются на лету: по клиенту, услуге, направлению и сотруднику, с трендом по месяцам.' }
]

export interface LandingFeature { title: string, text: string }
export const LANDING_FEATURES: LandingFeature[] = [
  { title: 'Спрашивать — прямо из сделки', text: 'Кнопка в карточке: ссылка готова, а вместе с ней сохраняется срез сделки, чтобы ответ потом лёг в нужную аналитику.' },
  { title: 'Цифры, а не ощущения', text: 'NPS и CSAT считаются сами: по клиенту, услуге, направлению и сотруднику, с трендом по месяцам.' },
  { title: 'Отвечать удобно', text: 'Анонимно, без входа, пара минут. Можно закрыть страницу и вернуться — прогресс сохранится.' },
  { title: 'Сравнивать версии честно', text: 'Опубликованная версия опроса неизменна, а вопросы связаны стабильными ключами — можно менять формулировки и всё равно сравнивать месяц с месяцем.' }
]

/**
 * Заголовки секций и текст о приватности.
 *
 * Лежат ЗДЕСЬ, а не в шаблоне, именно потому, что первая версия страницы держала их инлайн — и мимо
 * правки «обещать только отгруженное» проехали самые сильные утверждения: «три шага БЕЗ УЧАСТИЯ
 * ЧЕЛОВЕКА», «от стадии сделки… приложение проходит путь само» и «данные лежат на вашем сервере».
 * Тест `test/landing.test.ts` сканирует этот файл целиком, поэтому «единственный источник» стало
 * правдой, а не намерением.
 */
export const LANDING_SECTIONS = {
  steps: {
    kicker: 'Как это работает',
    title: 'Три шага от сделки до цифры',
    lead: 'Спросить клиента, получить ответ и увидеть цифру — без выгрузок и ручных подсчётов.'
  },
  features: { kicker: 'Возможности', title: 'Что получает руководитель' },
  privacy: {
    kicker: 'Приватность',
    title: 'Ответы клиентов защищены по умолчанию',
    // ⚠️ Про место хранения тут НЕ пишем: приложение облачное и работает у издателя. Обещать
    // «данные на вашем сервере» — расхождение и с моделью развёртывания, и с будущей политикой
    // конфиденциальности, которую придётся подать в Маркет.
    // ⚠️ Две правки против прежней редакции, обе — про несовпадение с кодом.
    // 1. «Опрос анонимный» без оговорки читалось как гарантия по всему продукту, а порог в пять
    //    ответов действует на ДАШБОРДЕ; возврат результата в карточку сделки идёт мимо него и включён
    //    по умолчанию (#18). Поэтому обещаем обезличенность АНАЛИТИКИ, а не опроса целиком.
    // 2. «Данные каждого портала живут в своей базе» перестало быть правдой с мультитенантом
    //    (#47/#49): база общая, изоляция — в слое доступа. Формулировка пережила свой код и уехала бы
    //    на живой домен вместе с первым же мержем.
    text: 'Аналитика обезличена: пока ответов меньше пяти, цифры не показываются вовсе — по маленькой выборке можно было бы узнать конкретного человека. Данные разных порталов изолированы и с чужими не смешиваются.'
  }
} as const

/** Боль → результат. */
export const LANDING_PAIN_RESULT = {
  before: 'Про качество работы узнают от тех, кто уже ушёл. Обратную связь собирают вручную, ответы теряются в переписке, сравнить месяц с месяцем нечем.',
  after: 'Клиента спрашивают сразу после работы, ответы копятся в одном месте, а руководитель открывает дашборд и видит обезличенную цифру — по услуге, клиенту и сотруднику.'
} as const

/** Код приложения в Маркете Битрикс24 — заготовка, слаг ещё не занят. */
export const LANDING_MARKET_CODE = 'shef.polls'


/**
 * Адрес карточки приложения в Маркете.
 *
 * ⚠️ Кнопки «Установить из Маркета» на лендинге сейчас НЕТ, и флага под неё тоже нет — намеренно
 * (решение владельца, 2026-08-22: прежний `MARKET_PUBLISHED` снят как лишняя ручка). Слаг не занят,
 * ссылка вела бы в 404 Маркета. В день публикации кнопка добавляется обычным PR — с
 * `pnpm test:visual:update` и сверкой глазами, потому что меняется разметка героя. Функция оставлена
 * ровно под этот день: адрес собирается из одного места.
 */
export const marketUrl = (code: string = LANDING_MARKET_CODE): string =>
  `https://www.bitrix24.ru/apps/app/${code}/`

/** Ключ демо-опроса, который открывается с лендинга (тот же, что в демо-сиде). */
export const LANDING_DEMO_SURVEY = 'csat_postdeal'

/** Публичный адрес лендинга. Константа, а не переменная окружения: канонический дом ровно один,
 *  а staging и клиентские сборки не должны объявлять себя оригиналом (урок #304 из соседнего проекта). */
export const LANDING_SITE_URL = 'https://polls.bx-shef.by'
