# Supply-chain: не выполнять lifecycle-скрипты зависимостей (postinstall и т.п.).
# pnpm 10 и так не запускает их без approve-builds — объявляем политику явно
# (и для случая запуска через npm/yarn).
ignore-scripts=true
