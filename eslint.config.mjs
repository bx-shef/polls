import withNuxt from './.nuxt/eslint.config.mjs'

export default withNuxt({
  ignores: [
    // Приманка для гейта границы слоёв: файлы нарушают правило намеренно,
    // линтеру и проверке типов их видеть незачем.
    'tests/fixtures/**',
    // Переносимые артефакты, не входящие в приложение: скрипт аудита живёт своей
    // жизнью и запускается отдельно, `legacy/` — справка только для чтения.
    // Переформатировать их под стиль приложения незачем: их читают и запускают снаружи.
    'tools/**',
    'legacy/**',
  ],
})
