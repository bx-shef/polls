<script setup lang="ts">
// Layout of the screens that live INSIDE the portal: `/app`, `/install`, `/portal/**`.
//
// ⚠ Имя `portal`, а не `default` или `clear`, и это не вкусовщина. У соседа это
// закреплено отдельным решением: разметка у портальных и служебных экранов одна, а мир
// разный — здесь iframe, фреймовый токен и слайдеры портала. Имя, которое не различает эти
// два случая, заставляет каждый раз выяснять заново, где ты находишься.
//
// ⚠ Умолчания (`layouts/default.vue`) у нас НЕТ намеренно. Страниц вне портала ровно две,
// и обе — отдельные миры со своей вёрсткой: лендинг и публичная анкета. Умолчание пришлось бы
// отключать на каждой из них, то есть писать `layout: false` там, где сейчас не написано
// ничего. Гвард на то, что портальная страница не забыла назвать layout, —
// `tests/unit/page-layouts.test.ts`.
</script>

<template>
  <AppShell>
    <slot />
  </AppShell>
</template>
