# Что делаем дальше

> Обновлено: 2026-08-07
>
> Этот файл — **список работ на будущее**: от ближайшего к «потом-потом». Как всё работает —
> в [`process.md`](./process.md). Что уже готово и в каком состоянии — в [`project-map.md`](./project-map.md).
>
> Живой трекер — [issues на GitHub](https://github.com/bx-shef/polls/issues); здесь — приоритеты и
> человеческие формулировки. Если файл и GitHub разошлись, **прав GitHub**. Прочерк в колонке «Трекер»
> означает, что issue ещё не заведена — задача живёт только здесь.
>
> Всё в этом файле по определению **⏳ отложено** — пометки статуса (✅ / 🔎 / ⏳) смотрите
> в [`project-map.md`](./project-map.md).

## Сейчас в работе — довести до боевого запуска

Это то, без чего сервис не включить на реальном портале.

| Что | Почему важно | Трекер |
|---|---|---|
| **Живой прогон на портале** — установка, handshake, виджеты, дашборд | Весь код связки написан и под тестами, но ни разу не проверен на реальном портале целиком. **Это главный блокер: он снимает риск со всего написанного разом.** Перед прогоном посмотрите лог запуска — проверка окружения назовёт неверные переменные до того, как они превратятся в непонятный 503 | — |
| **Живой прогон прод-стека** — запись → редеплой → чтение | Подтвердить, что данные переживают перезапуск (PostgreSQL разведён, но end-to-end не гонялся) | — |
| **Живой прогон авто-триггера** | Переход стадии подтверждается историей портала — сверить на реальном портале формат `CREATED_TIME` (смещение таймзоны) и `STAGE_ID` (префикс воронки): от них зависит, работает механизм или молча ничего не делает. Секция B2 в `scripts/b24-smoke.ts` сверяет оба формата и прогоняет их через само ядро | [#17](https://github.com/bx-shef/polls/issues/17) |
| **Одно приглашение на один переход** (защита от дублей) — **делать ДО доставки** | На пути «Событие» правки идут гроздью вокруг перехода (дозаполнение обязательных полей, автоматизация стадии) — на один переход выходит 2–4 приглашения. Бьёт трижды: спам клиенту заказчика, перекос NPS/CSAT (а аналитика и есть продукт) и лишний живой токен со снимком контекста на каждый дубль. Лечится идемпотентностью по `ID` записи истории стадий: он уникален для перехода и гасит всю гроздь. ⚠️ Нужен долговечный стор приглашений (предпосылка — [#4](https://github.com/bx-shef/polls/issues/4)) | [#138](https://github.com/bx-shef/polls/issues/138) |
| **Доставка приглашения через дело в таймлайне** | Ядро-конструктор готово; нужен постинг дела, виджет с кнопкой «Отправить приглашение» и живой прогон. ⚠️ **Блокируется [#138](https://github.com/bx-shef/polls/issues/138):** пока доставки нет, дубль — это дубль токена в памяти; как только приглашение пойдёт клиенту, тот же дефект станет боевым | [#126](https://github.com/bx-shef/polls/issues/126) |
| **Подключить токен приглашения к странице опроса** | Ссылка `?token=…` создаётся, но страница его не передаёт → привязка ответа к сделке, одноразовость и срок доступности в реальном пути клиента не работают (серверная часть готова) | — |
| **Возврат результата в таймлайн + просмотр результата** | Ядро готово. Нужен постинг на отправку и страница результата (HTML/печать/PDF). **Обязательно в скоупе:** привязка `responseId` к порталу (иначе чужой результат можно открыть перебором), **гейт постинга анонимностью опроса** (индивидуальный ответ в сделке противоречит обещанию «Анонимно»), экранирование текста при HTML-рендере | [#18](https://github.com/bx-shef/polls/issues/18) |
| **Проверка прав пользователя на сделку** в виджете | Сейчас любой пользователь портала может выписать ссылку на любую сделку — сверять с ответственным/участником. Флагом администратора это НЕ лечится: выписать приглашение — работа продавца, а не админа | — |
| **Гард «роуты записи обязаны звать гейт роли»** | Сейчас это проверяет текстовый тест по исходникам (`test/admin-gate.test.ts`) со списком роутов вручную. Новый роут записи можно завести, не добавив его в список, — стоит проверять каталог целиком, как `check:boundary` | — |

## Следующая очередь — удобство и охват

| Что | Зачем | Трекер |
|---|---|---|
| **Поле «срок доступности ссылки» в конструкторе** | Логика готова (5 минут — 5 дней), осталось вывести в интерфейс | — |
| **Выбор опроса для пары «направление + стадия»** в конструкторе | Сейчас это задаётся переменными окружения — неудобно и негибко | — |
| **Автотриггер для лида и смарт-процесса** | Ядро-маппер готов, нужен эндпоинт события и регистрация | — |
| **Живой smoke робота** | Эндпоинт и регистрация готовы — сверить реальный формат POST робота на портале | [#122](https://github.com/bx-shef/polls/issues/122) |
| **Изоляция нескольких порталов в одной базе** | Сейчас один инстанс обслуживает один портал | [#49](https://github.com/bx-shef/polls/issues/49) · [#47](https://github.com/bx-shef/polls/issues/47) |
| **Тексты контура A и дашборда** + перегенерация эталонов | Довести до конца работу над понятными сообщениями | [#130](https://github.com/bx-shef/polls/issues/130) |
| **Кнопка «очистить данные за период»** | Сейчас это ручная SQL-операция | — |
| **CES на дашборде** | Метрика CES считается в ядре, но дашборд её ещё не выводит (только NPS/CSAT/распределения) | — |
| **Интеракционные состояния и админка под визуальным гейтом** | Наведение/фокус/выключено + экраны `/admin/*` не сняты эталонами | [#118](https://github.com/bx-shef/polls/issues/118) · [#13](https://github.com/bx-shef/polls/issues/13) |
| **Обобщить ключ стадии** (`dealStageId` → `entityStageId`) | Имя историческое, поле уже общее для сделки/лида/смарт-процесса. ⚠️ Блокер: поле лежит в JSONB живой базы → нужна миграция данных и совместимость на чтение | — |
| **Составная фильтрация триггеров** `(тип сущности, стадия)` | У смарт-процессов свой namespace стадий — сейчас поиск идёт только по строке стадии | — |
| **Товары для лида и смарт-процесса** + группировка услуг без каталога | Сейчас срез «услуга» есть только у сделки, позиции без товара в каталоге теряются | — |

## Продукт — чтобы продавать

| Что | Зачем | Трекер |
|---|---|---|
| **Лендинг** | Публичная витрина: что делает приложение, демо, кнопка «Установить из Маркета». Сейчас корень — технический каркас | — |
| **Страница оператора (владельца)** | Служебная консоль: проверка рейтинга в Маркете, подтверждение отзывов | — |
| **Попап «оцените приложение»** | Предложение оценить после успешного прохождения, с переходом в Маркет | — |
| **Включить канал отзывов на проде** | Виджет 👍/👎 готов и стоит на дашборде и в конструкторе. Осталось: завести приватный репозиторий-приёмник, выпустить fine-grained токен (право `Issues: write` только на него), задать `GITHUB_FEEDBACK_*` и перезапустить. Плюс поставить виджет на виджеты карточки сделки (`/b24/*`) | — |

Референс для всех четырёх пунктов — соседний проект `ai-price-import`, там это уже работает:
лендинг — `app/pages/index.vue` + `app/utils/landing.ts`; страница оператора — `app/pages/queues.vue`;
попап рейтинга — `AppRatingModal.vue` + `useAppRating` + `server/api/app-rating.*` и `ops/app-rating.*`;
виджет обратной связи — `app/components/FeedbackWidget.vue` (в `ai-price-import`; у нас теперь есть свой).

## Эксплуатация и надёжность

| Что | Зачем | Трекер |
|---|---|---|
| **Наблюдаемость на проде** — Pino, Sentry, метрики, OpenTelemetry | Сейчас есть только свой логгер; на проде нужны нормальные адаптеры и трейсы | [#15](https://github.com/bx-shef/polls/issues/15) |
| **Общее хранилище анти-абьюза** (Redis/PostgreSQL) | Чтобы лимиты и одноразовые токены работали при нескольких инстансах. ⚠️ Хранилище приглашений обязано сохранять срок жизни ссылки, иначе он сбросится после перезапуска. Оно же — предпосылка защиты от дублей ([#138](https://github.com/bx-shef/polls/issues/138)): ключ идемпотентности перехода не переживёт перезапуск в памяти | [#4](https://github.com/bx-shef/polls/issues/4) |
| **Визуальный гейт в CI** | Сейчас скриншот-регрессия запускается локально хуком; нужен закреплённый контейнер рендера | [#41](https://github.com/bx-shef/polls/issues/41) |
| **Удаление персональных данных на уровне API** | Пока только ручной SQL. Чистить нужно всё: `response.context` (JSONB — там имена), денормализованные колонки (`contact_id`, `company_id`, `responsible_id`) и `response_product.product_name` | [#31](https://github.com/bx-shef/polls/issues/31) · [#10](https://github.com/bx-shef/polls/issues/10) |
| **Явный список зон в allowlist домена** + проверка адреса сервера авторизации | Сейчас зона портала проверяется по форме (`bitrix24.<зона>`), а не по списку — остаточный риск SSRF | — |
| **Уникальность домена портала в базе** | Защита от подмены домена (domain-poisoning) при установке | — |
| **Политика IP и заголовка `X-Forwarded-For`** | За прокси лимит по IP вырождается в общий счётчик, а слепое доверие заголовку позволяет его подделать. Плюс IP — персональные данные: решить, хешируем или не пишем | — |
| **Публикация образа только после зелёного CI** | Сейчас образ уезжает в GHCR по факту мержа, независимо от результата проверок | — |
| **Ограничение размера тела без nginx** | Заголовки безопасности уже в приложении; осталось ограничение размера тела на уровне процесса (413 на превышение, 411 на тело без длины — до чтения тела) для случая работы без прокси. Референс — `edgeBodyGuard` в `ai-price-import` | — |
| **`frame-ancestors` из домена установленного портала** | Сейчас список зон Bitrix24 захардкожен, а allowlist установки принимает любую зону: портал в незалистенной зоне установится и получит пустой фрейм. Правильно — выводить источник из домена портала, который открыл фрейм (он есть в базе), сверяя тем же `isAllowedPortalDomain`. **Гейт выхода в Маркет** — там порталы со всего мира | — |
| **nonce вместо `'unsafe-inline'` в CSP** | Сейчас политика закрывает кликджекинг и внешние источники, но не XSS: инлайн-скрипт разрешён, потому что через него Nuxt отдаёт состояние гидратации. Ужесточение требует правок рендера | — |
| **Альтернативный деплой — Bitrix24 Vibecode** | Запуск одним процессом в закрытой среде Bitrix (референс — `ai-price-import`, `docs/DEPLOY_VIBECODE.md`) | — |

## Потом-потом — большие идеи

Ничего из этого не начато; записано, чтобы не потерять.

- **AI-разбор ответов** — превращать свободный текст («Другое», комментарии) в число + причину + следующее
  действие: тема, тональность, намерение (вернуть клиента / допродажа / похвала / баг). Место в базе уже
  подготовлено (`answer_insight`). Это и есть главная ценность продукта — «продаём решения, а не анкеты».
- **Петля обратной связи в CRM** — недовольный клиент автоматически создаёт задачу на возврат, довольный —
  запрос отзыва.
- **Публикация агрегатов в Ленту портала** — регулярная сводка по NPS/CSAT без выгрузок.
- **Опросы сотрудников (eNPS / пульс)** — анонимный сбор внутри портала через чат-бота, в Ленту только
  агрегаты.
- **Больше точек встройки** — вкладка сделки, лиды и контакты, левое меню, доставка через чат-бота.
- **Прогрессивный дашборд** — первый экран только NPS/CSAT и топ-срезы, детали по нажатию.
- **Мультиязычность опросов** — сейчас один опрос = один язык; платформа к этому готова.
- **Перетаскивание вопросов мышью** в конструкторе — сейчас порядок меняется кнопками.
- **Группировка услуг без каталога** — сейчас позиции без товара в каталоге в срез «услуга» не попадают.

---

*Как всё устроено — [`project-map.md`](./project-map.md). Как работает процесс — [`process.md`](./process.md).*
