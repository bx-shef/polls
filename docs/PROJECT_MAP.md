# Карта проекта

Состояние частей одной фразой. Обновляется по мере работы, а не раз в квартал.

Обозначения: ✅ проверено на живом портале · 🧪 сделано, живьём не проверено · 🔨 в работе ·
⏸️ отложено осознанно · ⛔ блокер · ⬜ не начато

---

## Части

| Часть | Состояние | Одной фразой |
|---|---|---|
| Каркас, деплой, CI | ✅ | Nuxt 4 + Nitro, Postgres, Redis, Docker, `pnpm check` в CI; выкачен на `polls.bx-shef.by`, здоровье и заголовки проверены живьём. Приложение с этого адреса поставлено на боевой портал и отработало сквозной сценарий 21–22.09 |
| Установка на портал | ✅ | ДВА пути: мастер во фрейме (рабочий для Маркета — при пункте в меню `ONAPPINSTALL` не приходит) и событие (для режима без меню). Верификация `member_id` переавторизацией общая, поверх рабочего портала грант обязан доказать права администратора; шифрованные токены, идемпотентное создание смарт-процессов и полей, доустройство отдельным вызовом. **Проверено на живом портале 2026-09-18**: токены сохранены, смарт-процессы и поля созданы, портал `active`. Событие `ONAPPINSTALL` после `installFinish()` НЕ приходит — значит `application_token` у нас нет и `ONAPPUNINSTALL` проверить нечем; tombstone нет |
| Смарт-процессы и поля | ✅ | «Опрос» и «Шаблон опроса» создаются при установке идемпотентно; без прав администратора портал сохраняется как `degraded`. Живьём проверены и связь «Опроса» со сделкой, и раскладка карточки: портал принимает флаг списка как `'true'`, а возвращает `'Y'` — расхождение разобрано в `PROCESS.md` |
| Конструктор и версии | 🧪 | вкладка «Конструктор» в карточке «Шаблона опроса» (#74): разделы, вопросы, шкалы, веса, диапазоны; правка черновика с защитой от чужой правки (#78), проверка схемы перед публикацией — дыры в диапазонах публикацию запрещают (#77), публикация и новая версия от опубликованной (#79). НЕ сделано: библиотека вопросов, настройки уровня портала |
| Публичная страница | ✅ | `/s/[token]` рендерит анкету и принимает ответ в буфер; токен, хеш, TTL, одноразовость, ограничение частоты по адресу и по токену, свой CSP с nonce вместо `unsafe-inline` (#52) и `noindex`. **Пройдено с телефона 21–22.09**: анкета открылась, ответ ушёл, итог показан |
| Выпуск и учёт ссылок | 🔨 | из карточки сделки: вкладка на `b24ui`, выбор анкеты, ссылка со сроком 30 дней, схема версии уезжает в кэш тем же действием — **выпуск пройден на живом портале 21–22.09**; список выпущенных со статусами, отзыв и перевыпуск (#61). НЕ сделано: роботом, пачкой |
| Результат в карточке «Опроса» | 🧪 | поле своего типа «Результат опроса»: анкета словами, нередактируемо по построению. Первый шаг — виджет НАД двумя JSON-полями; на установленных порталах встаёт в наш раздел раскладки ревизией обустройства 3, на новых — после `installFinish`. JSON уходит вторым шагом, после живой проверки — пять вопросов к ней в `PROCESS.md`, раздел 9 |
| Роботы БП | ⬜ | «Создать ссылку» и «Опрос с ожиданием ответа» через `bizproc.event.send` |
| Триггер по стадии | ⬜ | подписка на `ONCRM*UPDATE` плюс сравнение с последней известной стадией и дедуп |
| Запись в CRM | 🔨 | буфер разбирается: ответы и баллы уезжают в элемент «Опрос», в таймлайн сделки — балл и ответы целиком; последний балл и дата — в поля сделки и контакта (#55); `FOR UPDATE SKIP LOCKED`, экспоненциальная пауза, возврат подвисших. Пройдено на живом портале 21–22.09: ссылка, ответ с телефона, дело в сделке |
| Напоминания | ⬜ | повторное уведомление по невыполненным приглашениям |
| Отчётность | ⬜ | три уровня: сделка, компания и контакт, сводный отчёт |
| Уведомления и чат-бот | ⬜ | `imbot.v2`, два канала: рабочий и технических проблем |
| Состояния портала и деградация | 🧪 | Жизнь портала замкнута: мёртвый грант отмечается первым отказом, снимается успешным продлением, через 30 суток уборщик стирает токены и ставит `deleted`. Продление — compare-and-swap, гонка двух обменщиков закрыта. НЕ сделано: тексты деградации для трёх адресатов, буфер при недоступном портале |
| Импорт из старого решения | ✅ | Снимок снят и разобран: 12 анкет из 12 при 11 предупреждениях. `pnpm migrate:templates` пишет их в «Шаблон опроса» входящим вебхуком, `pnpm publish:templates` называет и публикует — оба с сухим прогоном по умолчанию. **Проверено на живом портале 2026-09-22**: 12 записано, повторный прогон создал ноль. Названий в источнике нет вовсе, поэтому анкету обязан назвать владелец: имя элемента уезжает в схему при публикации, и только так респондент его увидит. Карточки и ответы прошлых опросов не переносятся: они остаются в старом решении |
| Лендинг и справка | 🔨 | лендинг на `/` есть: суть продукта, три шага, отличия от формы, SSR и полная мета — тексты из задания дизайнеру, чтобы карточка Маркета и лендинг не разошлись. Справка `/help` и `/llms.txt` из одного источника (`shared/faq.ts`), кнопки «Что это значит?» открывают её слайдером портала на нужном разделе. Нет служебной страницы издателя и контакта поддержки |
| Юридический пакет | ⬜ | четыре документа двумя парами, архив редакций с `sha256` |
| Карточка Маркета | ⬜ | тексты готовы (`docs/market-graphics.md`), знак и изображения — нет |

---

## Что мешает выпуску в Маркет

- ~~**Цепочка сомкнута с одного конца.**~~ Снято: все пять звеньев на месте. Задача 7
  (`pnpm migrate:templates`) записывает разобранные шаблоны в смарт-процесс «Шаблон опроса»
  черновиками; сухой прогон по умолчанию, повторный запуск ничего не перезаписывает.
  На настоящем снимке заказчика разбирается 12 анкет из 12 при 11 предупреждениях.
  **Что осталось руками:** назвать анкеты — в источнике человеческих названий нет вовсе,
  поэтому после переноса они называются своими кодами и ждут публикации.
- ~~**Хост.**~~ Снято: приложение работает на `polls.bx-shef.by` за общим `nginx-proxy`,
  сертификат выпущен, `/api/health` отвечает `ok` по базе и Redis. Живьём проверены
  портальная CSP со всеми региональными зонами, узкая политика на `/api/**`, HSTS и
  `Referrer-Policy`. Задача 4 больше ничем не заблокирована.
- **Общая инфраструктура хоста.** Стек рассчитан на уже работающие `nginx-proxy`,
  `acme-companion` и Watchtower — как у `client-bank-alfa-by`. Сеть `proxy-net` объявлена
  внешней: без неё `compose` не запустится вовсе. Обмен осознанный — движущихся частей
  у нас меньше, но TLS, сертификаты и формат access-логов больше не наша зона.
- **Лимит роботов на Базовом тарифе.** В тарифной таблице «Роботы и триггеры — 5», и неясно,
  считается лимит на стадию, на воронку или суммарно. Мы регистрируем двух. Проверить на живом
  Базовом портале до того, как обещать клиенту оба робота.
- **Права администратора.** Без них не создать смарт-процессы, поля и роботов. Проверяется при
  установке, не позже.
- Скриншоты для карточки Маркета снять неоткуда, пока не пройдёт сквозной сценарий.

---

## Известные ограничения

- Отдельного события «стадия изменилась» в Битрикс24 нет: разница считается сравнением с последним
  известным состоянием, поэтому дедуп обязателен.
- `app.option.set`, `bizproc.robot.add` и `crm.type.add` требуют прав администратора.
- Смарт-процессы недоступны на бесплатном тарифе, но там и приложения Маркета не ставятся —
  практически ограничение не срабатывает.
- Технической проверки подписки Маркета в API нет; реагируем на ошибку, а не проверяем заранее.

---

## Чек-лист приёмки первых суток

Подробности — в `DAY-ONE.md`. Коротко: менеджер выпускает ссылку из карточки, человек проходит
анкету с телефона, ответ и балл появляются в карточке сделки, повторная отправка отбивается,
истёкшая ссылка объясняет причину.

_Last reviewed: 2026-09-26_
