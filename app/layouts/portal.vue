<script setup lang="ts">
// Layout of the screens that live INSIDE the portal: `/app`, `/install`, `/portal/**`, `/uf/**` —
// and of `/help`, the one public page that wears it.
//
// ⚠ `/help` — ИСКЛЮЧЕНИЕ, и о нём надо помнить, трогая этот layout или `AppShell`. Справка
// публичная и рендерится на сервере (её читают поиск и модератор Маркета), а внутри портала
// показывается в слайдере — и портальный вид нужен ей ради второго. Значит, здесь не может
// появиться ничего, что требует фрейма: инициализация SDK, гейт присутствия в портале, `noindex`.
// Всё это сломало бы серверный рендер справки молча. Нашли `/review` и `/code-review` в PR #82.
//
// ⚠ Имя `portal`, а не `default` или `clear`, и это не вкусовщина. У соседа это
// закреплено отдельным решением: разметка у портальных и служебных экранов одна, а мир
// разный — здесь iframe, фреймовый токен и слайдеры портала. Имя, которое не различает эти
// два случая, заставляет каждый раз выяснять заново, где ты находишься.
//
// ⚠ Умолчания (`layouts/default.vue`) у нас НЕТ намеренно. Страниц со СВОЕЙ вёрсткой ровно две,
// и обе — отдельные миры: лендинг и публичная анкета. Умолчание пришлось бы отключать на каждой
// из них, то есть писать `layout: false` там, где сейчас не написано ничего. Гвард на то,
// что портальная страница не забыла назвать layout, — `tests/unit/page-layouts.test.ts`.
</script>

<template>
  <AppShell>
    <slot />
  </AppShell>
</template>
