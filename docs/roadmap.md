# Карта фаз

> Где мы сейчас и куда движемся. North-star — рабочий сервис опросов в связке с
> Bitrix24 (датчик → AI → KPI/Лента). Спецификация — [`brief.md`](./brief.md),
> состояние слоёв «одним взглядом» — в [`../CLAUDE.md`](../CLAUDE.md).

## Где мы

✅ **Ядро** (`domain` / `store` / `api` / `obs` / `bitrix24` / `server`) — готово под
тестами (покрытие ~100% + `pnpm verify`). Это framework-agnostic движок + HTTP-слой
с анти-абьюзом, PgStore, OAuth-ядром Bitrix24, наблюдаемостью.

✅ **Контур A начат:** `client/survey-fill.ts` (`SurveyFill`) — framework-agnostic
«мозг» прохождения опроса (навигация/deep-link, валидация шага, single/multi +
exclusive, «Другое», persist-снимок, маппинг в `Submission`), под тестами (#24).
Без DOM/Vue — экраны и Vue-композабл оборачивают его в фазе связки.

## Куда движемся (фазы связки и деплоя)

```
[✅ ядро] → [✅ SurveyFill (мозг контура A)] → [⏳ Nuxt+b24ui каркас]
              → [⏳ Nitro-привязка createApi] → [⏳ экраны опроса (контур A)]
                  → [⏳ дашборд (контур B)] → [⏳ деплой-слой]
```

| Фаза | Что | Зависит от | Issue |
|---|---|---|---|
| 0 | `SurveyFill` — «мозг» прохождения (контур A) | — | ✅ #24 |
| 0.5 | Презентационные поля в схеме (`intro`/`thanks`/`blocks`) | — | ✅ #25 |
| 0.6 | Read-API текущей версии: `GET /api/survey/:key/current` (хендлер + node-роутинг, проекция без `invitationPolicy`) | 0.5 | ✅ (хвост #10) |
| 1 | Nuxt 4 + b24ui каркас (`templates-dashboard`) | — | — |
| 2 | Nitro-привязка `createApi` (эндпоинты уже есть; остаётся обёртка + кэш/ETag) | 1 | #10 (частично), #30 (кэш/ETag) |
| 3 | Экраны опроса (контур A): Интро/Опрос/Спасибо поверх `SurveyFill` | 0, 0.5, 1, 2 | #13 (виз. гейт) |
| 4 | Bitrix24-связка: invitation binding + результат в таймлайн | 2, 3 | #17, #18 |
| 5 | Дашборд результатов (контур B): аналитика внутри Bitrix24 | 1, 3 | — |
| 6 | Деплой-слой: Docker/TLS/мульти-инстанс/наблюдаемость | 2 | #4, #15, #10 (хвост) |

**Параллельно ядру** (не блокируют фронт): durable-идемпотентность `addResponse`
(invitation_token, 0003) и SQL-`npsTrend` (`aggregateNpsTrend`) — ✅; остаётся общий
стор анти-абьюза (#4), PII-редакция на HTTP-слое (#31), binding `ONCRMDEALUPDATE`
(#17). Подробности и зависимости — [`issues.md`](./issues.md).

## Принцип очерёдности

Сначала контур A (публичный сбор) — он даёт работающий датчик и визуальную сверку с
`design.md`. Контур B (дашборд) переиспользует тот же движок агрегации (`domain/aggregate`).
Деплой-слой подключается, когда есть что деплоить (фронт + Nitro).

---
*Последнее ревью: 2026-06-19.*
