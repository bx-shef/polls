<script setup lang="ts">
/**
 * The application root. Nothing but the layout switch and the global palette.
 *
 * ⚠ SEO-меты здесь нет и быть не должно. Корень применяется ко ВСЕМ страницам, включая
 * служебные и публичную анкету; мета, поставленная отсюда, уносила бы в выдачу заголовок
 * и описание лендинга с каждой из них. У соседа это подтвердилось на живом проде. Мета живёт
 * на самих страницах, служебные закрыты заголовком `X-Robots-Tag`.
 */
</script>

<template>
  <NuxtLayout>
    <NuxtPage />
  </NuxtLayout>
</template>

<style>
/*
  ⚠ ПРИЛОЖЕНИЕ СВЕТЛОЕ ВСЕГДА — решение владельца 24.09, и `color-scheme: light` здесь
  не украшение, а сама механика. Одного удаления блока `prefers-color-scheme` было бы мало:
  без явного значения браузер на тёмной системе всё равно красит СВОЁ — полосы прокрутки,
  выпадающие списки, календарь у `input[type=date]`, автозаполнение, — и на белой странице
  это выглядит как поехавшая вёрстка, а не как тема. Одна строка закрывает весь этот класс.

  Почему вообще отказались от тёмной: наши экраны живут в iframe чужого портала, а Битрикс24
  своей темой с нами не делится — фрейм её не сообщает. То есть «тёмная тема» у нас была
  не темой портала, а темой ОПЕРАЦИОННОЙ СИСТЕМЫ сотрудника: тёмный лист приложения внутри
  светлого портала. Набор `b24ui` при этом уже светлый — его тёмный вариант включается
  классом `.dark`, которого у нас не ставит никто, — так что тёмной была ровно эта палитра
  и ровно наполовину.

  Публичная анкета сюда НЕ относится: она тёмная по макету заказчика и объявляет свою схему
  сама, см. `app/pages/s/[token].vue`.
*/
:root {
  color-scheme: light;
  --bg: #ffffff;
  --fg: #17181a;
  --muted: #6b7280;
  --line: #e5e7eb;
  --ok: #1f9254;
  --bad: #c0392b;
  /* Добавлено лендингом: подложка для нумерации и значков, цвет ссылки. Роли, а не значения —
     правило проекта. Лендинг сначала завёл собственный набор из восемнадцати литералов,
     и он уже разъехался с этим (`#e6e6e6` против `--fg: #eceff3`). Нашла панель ревью PR #28. */
  --surface: #eef2f7;
  --accent: #1f6feb;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  background: var(--bg);
  color: var(--fg);
  font: 15px/1.55 system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
}
</style>
